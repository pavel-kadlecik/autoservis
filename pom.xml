<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.3</version>
		<relativePath/>
	</parent>

	<groupId>cz.palo</groupId>
	<artifactId>autoservis</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>autoservis</name>
	<description>Auto service management system — Spring Boot + MyBatis + PostgreSQL + React</description>

	<properties>
		<java.version>21</java.version>

		<!-- Database credentials for the Flyway Maven plugin (CLI: ./mvnw flyway:migrate).
		     Password comes from the DB_PASSWORD env var so it is not committed.
		     Run e.g.: DB_PASSWORD=yourpass ./mvnw flyway:info -->
		<db.url>jdbc:postgresql://localhost:5432/autoservis</db.url>
		<db.username>postgres</db.username>
		<!--suppress UnresolvedMavenProperty -->
		<db.password>${env.DB_PASSWORD}</db.password>

		<spring-ai.version>2.0.0-M4</spring-ai.version>
	</properties>

	<!-- Testcontainers BOM — centralised version management -->
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.testcontainers</groupId>
				<artifactId>testcontainers-bom</artifactId>
				<version>1.21.0</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.springframework.ai</groupId>
				<artifactId>spring-ai-bom</artifactId>
				<version>${spring-ai.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.mybatis.spring.boot</groupId>
			<artifactId>mybatis-spring-boot-starter</artifactId>
			<version>4.0.1</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<!-- Odesílání faktur e-mailem přes SMTP (Seznam) — JavaMailSender -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-model-anthropic</artifactId>
		</dependency>

		<!-- PostgreSQL JDBC driver -->
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Lombok — compile-time boilerplate generation -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<!-- JWT — jjwt library -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.13.0</version>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.13.0</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.13.0</version>
			<scope>runtime</scope>
		</dependency>

		<!-- Test dependencies -->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mybatis.spring.boot</groupId>
			<artifactId>mybatis-spring-boot-starter-test</artifactId>
			<version>4.0.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
		</dependency>
		<dependency>
		<groupId>io.github.openhtmltopdf</groupId>
		<artifactId>openhtmltopdf-pdfbox</artifactId>
		<version>1.1.40</version>
		</dependency>

        <!-- QR kód (QR Platba / SPAYD) -->
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>core</artifactId>
            <version>3.5.4</version>
        </dependency>
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>javase</artifactId>
            <version>3.5.4</version>
        </dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<!-- Flyway Maven plugin — allows running migrations from the command line: ./mvnw flyway:migrate -->
			<plugin>
				<groupId>org.flywaydb</groupId>
				<artifactId>flyway-maven-plugin</artifactId>
				<version>11.14.1</version>
				<configuration>
					<url>${db.url}</url>
					<user>${db.username}</user>
					<password>${db.password}</password>
					<defaultSchema>public</defaultSchema>
					<!-- Lokální CLI (./mvnw flyway:*) = dev-like: schéma + demo data.
					     Produkční seed (db/prod) se přes CLI nespouští — ten řídí app profil prod.

					     filesystem:, ne classpath: — `classpath:` čte migrace z target/classes,
					     jenže flyway:* jsou samostatné cíle a `process-resources` před sebou
					     nespouštějí. Po úpravě migrace tak plugin pracoval se starou kopií:
					     flyway:repair uložil otisk STARÉHO souboru, aplikace pak překopírovala
					     nový a spadla na „checksum mismatch" (2026-08-06). Čtením přímo ze
					     zdrojů to odpadá a nikdo si nemusí pamatovat `process-resources`.
					     Běhu aplikace se to netýká — ta si locations bere z application.yaml
					     (spring.flyway.locations), na této konfiguraci nezávisle. -->
					<locations>
						<location>filesystem:${project.basedir}/src/main/resources/db/migration</location>
						<location>filesystem:${project.basedir}/src/main/resources/db/demo</location>
					</locations>
					<cleanDisabled>true</cleanDisabled>
					<validateOnMigrate>true</validateOnMigrate>
					<outOfOrder>false</outOfOrder>
				</configuration>
				<dependencies>
					<dependency>
						<groupId>org.postgresql</groupId>
						<artifactId>postgresql</artifactId>
						<version>42.7.10</version>
					</dependency>
					<dependency>
						<groupId>org.flywaydb</groupId>
						<artifactId>flyway-database-postgresql</artifactId>
						<version>11.14.1</version>
					</dependency>
				</dependencies>
			</plugin>

			<!-- JaCoCo — pokrytí testy. Cíl je VĚTVOVÉ pokrytí (TD-14), ne řádková procenta.
			     Agent se připne na test běh, report se vygeneruje po testech do target/site/jacoco,
			     poté `check` vynutí prahy. Prahy jsou nastavené POD reálně dosaženou hodnotou
			     (2026-07-23: instrukce 84,5 %, větve 73,9 %) s rezervou, aby build shodila až
			     skutečná regrese, ne kolísání o pár řádků. Zvyšovat s přibývajícím pokrytím. -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.13</version>
				<executions>
					<execution>
						<id>jacoco-prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>jacoco-report</id>
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
					<execution>
						<id>jacoco-check</id>
						<phase>test</phase>
						<goals>
							<goal>check</goal>
						</goals>
						<configuration>
							<rules>
								<!-- Celý projekt: hlídá regresi celkového pokrytí. -->
								<rule>
									<element>BUNDLE</element>
									<limits>
										<limit>
											<counter>INSTRUCTION</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.80</minimum>
										</limit>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.68</minimum>
										</limit>
									</limits>
								</rule>
								<!-- Kritické balíčky: větvové pokrytí nesmí spadnout pod dosažený základ. -->
								<rule>
									<element>PACKAGE</element>
									<includes>
										<include>cz.palo.autoservis.service</include>
										<include>cz.palo.autoservis.service.impl</include>
										<include>cz.palo.autoservis.security.service</include>
										<include>cz.palo.autoservis.model.converter</include>
									</includes>
									<limits>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.65</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- PIT — mutační testování. Důkaz, že testy nejsou plané (přeživší mutant = díra v asercích).
			     Omezeno na kritické balíčky service + security (běh na celém projektu by byl pomalý).
			     Spouští se RUČNĚ, ne v každém test běhu:
			       ./mvnw test-compile org.pitest:pitest-maven:mutationCoverage -->
			<plugin>
				<groupId>org.pitest</groupId>
				<artifactId>pitest-maven</artifactId>
				<version>1.20.3</version>
				<dependencies>
					<dependency>
						<groupId>org.pitest</groupId>
						<artifactId>pitest-junit5-plugin</artifactId>
						<version>1.2.3</version>
					</dependency>
				</dependencies>
				<configuration>
					<targetClasses>
						<param>cz.palo.autoservis.service.*</param>
						<param>cz.palo.autoservis.security.service.*</param>
						<!-- Konvertory a enumy doplněny 2026-07-31 (audit KN-21): žijí v nich
						     stavové automaty a přepisy hodnot z formulářů, tedy přesně třída chyb,
						     jakou byl KN-9 (nulová sazba DPH tiše na 21 %) — PIT ji odhalí sám. -->
						<param>cz.palo.autoservis.model.converter.*</param>
						<param>cz.palo.autoservis.model.enums.*</param>
					</targetClasses>
					<targetTests>
						<param>cz.palo.autoservis.*</param>
					</targetTests>
					<!-- Manuální a externí testy z mutačního běhu ven (reálné API / mimo CI). -->
					<excludedTestClasses>
						<param>cz.palo.autoservis.service.PdfDocumentExtractionManualTest</param>
					</excludedTestClasses>
					<outputFormats>
						<param>HTML</param>
						<param>XML</param>
					</outputFormats>
					<timestampedReports>false</timestampedReports>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<repositories>
		<repository>
			<id>spring-milestones</id>
			<url>https://repo.spring.io/milestone</url>
			<snapshots><enabled>false</enabled></snapshots>
		</repository>
	</repositories>

</project>
