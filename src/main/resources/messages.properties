# Human-readable messages resolved by GlobalExceptionHandler for custom Bean Validation
# annotations whose violation template carries a machine-readable error code
# (see GlobalExceptionHandler.CUSTOM_VALIDATOR_ANNOTATIONS). Czech — this API has no
# i18n requirement (CLAUDE.md: komunikace s uzivatelem cesky), so there is no
# messages_cs.properties variant; this file IS the (default) locale.

# ValidCustomerRequest (TD-10)
CUSTOMER_NAME_REQUIRED=Jméno a příjmení jsou u fyzické osoby povinné
CUSTOMER_COMPANY_REQUIRED=Název firmy je u firmy povinný
